<Window x:Class="DreamLauncher.Windows.Dialogs.ModpackImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入整合包"
        Width="420"
        Height="260"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        Foreground="{StaticResource TextPrimary}">
    <Border Margin="10"
            Padding="20"
            Background="{StaticResource PanelBackground}"
            BorderBrush="{StaticResource StrokeSoft}"
            BorderThickness="1"
            CornerRadius="20"
            MouseLeftButtonDown="Dialog_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Background="Transparent" MouseLeftButtonDown="Dialog_MouseLeftButtonDown">
                <TextBlock Text="导入整合包" FontSize="20" FontWeight="Bold" />
            </Grid>
            <TextBlock Grid.Row="1"
                       Text="请输入实例名称，将作为版本文件夹名。"
                       Foreground="{StaticResource TextMuted}"
                       TextWrapping="Wrap"
                       Margin="0,8,0,18" />

            <StackPanel Grid.Row="2">
                <TextBlock Text="实例名称" Style="{StaticResource FieldLabel}" />
                <TextBox x:Name="InstanceNameTextBox"
                         Style="{StaticResource LauncherTextBox}"
                         MaxLength="64" />
                <TextBlock Text="建议使用英文、数字或下划线，避免特殊字符"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="12"
                           Margin="0,8,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" />
                <Button Content="导入" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="Import_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
